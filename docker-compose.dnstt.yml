# ======================================================
#  [已廢棄] 此檔案已整合進 docker-compose.yml
#
#  請改用以下指令啟動 DNSTT DNS 隧道服務：
#    docker compose --profile dnstt up -d
#
#  詳細說明請參閱 docker-compose.yml 檔案頂部的說明。
# ======================================================
